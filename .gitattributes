# Normalize line endings to LF in the repository and in every working tree.
# Without this, core.autocrlf=true on Windows checks files out as CRLF while
# Biome and the repository content both expect LF, so `pnpm lint` reports
# format violations locally that CI never sees.
* text=auto eol=lf
