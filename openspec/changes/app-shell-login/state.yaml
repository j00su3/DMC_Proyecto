# SDD DAG state for change: app-shell-login (survives compaction)
schema: spec-driven
change: app-shell-login
project: inventienda
backlog_item: "2.1"
artifact_store: hybrid   # phases save to BOTH Engram and OpenSpec files
updated: 2026-08-25

session_preflight:
  execution_mode: interactive
  artifact_store: hybrid
  delivery_strategy: ask-on-risk
  chain_strategy: stacked-to-main
  review_budget_lines: 400   # authored lines; generated/lockfile excluded

# ask-on-risk fired on the tasks forecast: S4 (~410) and S5a (~430) are both
# marginally over budget. User decision, 2026-08-25: SHIP THEM AS ONE PR EACH.
# No further splitting beyond the S5 -> S5a/S5b split already in tasks.md.
# Rationale: the budget protects reviewer attention, and a 430-line diff is read
# as well as a 400-line one. What breaks review is a jump in scale — S5 whole
# was ~770, and the previous cycle's overrun was 810 — not a 7% overage. Chained
# PRs also carry real cost: the auth cycle lost a PR when its base branch was
# deleted (GitHub closes, it does not retarget) and it had to be restored,
# reopened and retargeted by hand. Splitting S4 and S5a would pay all of that
# ceremony for nothing.
delivery_decision:
  policy: no-further-splits
  pr_count: 7
  decided: 2026-08-25
  # The chain actually shipped as 9 PRs, not the 7 forecast. The two extra are
  # not seam splits and do not reopen the no-further-splits decision: #16 is the
  # planning-docs PR that predates the seams, and #20 is the integration-test
  # isolation fix, which was kept INSIDE the chain rather than branched off main
  # so every downstream PR runs CI against the fixed config.
  pull_requests:
    opened: 2026-08-26
    merge_order: strictly bottom-up, #16 first
    warning: >-
      NEVER delete a base branch while a dependent PR is open — GitHub closes
      the dependent instead of retargeting it. This already cost a PR during the
      auth-sesiones cycle. Retarget each PR to main immediately before merging.
    chain:
      - "#16 docs/sdd-planning-frontend        <- main"
      - "#17 feat/shell-pr1-api-errors         <- docs/sdd-planning-frontend"
      - "#18 feat/shell-pr2-data-domain        <- feat/shell-pr1-api-errors"
      - "#19 feat/shell-pr3-api-guard          <- feat/shell-pr2-data-domain"
      - "#20 fix/integration-test-isolation    <- feat/shell-pr3-api-guard"
      - "#21 feat/shell-pr4-visual-foundation  <- fix/integration-test-isolation"
      - "#22 feat/shell-pr5a-router-session    <- feat/shell-pr4-visual-foundation"
      - "#23 feat/shell-pr5b-login-screen      <- feat/shell-pr5a-router-session"
      - "#24 feat/shell-pr6-change-password    <- feat/shell-pr5b-login-screen"

phases:
  explore:
    status: completed
    artifact: exploration.md
    engram_topic: sdd/app-shell-login/explore
  proposal:
    status: completed
    artifact: proposal.md
    engram_topic: sdd/app-shell-login/proposal
  spec:
    status: completed
    artifacts:
      - specs/app-shell/spec.md          # NEW capability
      - specs/password-change/spec.md    # NEW capability
      - specs/auth-sessions/spec.md      # DELTA, 4 MODIFIED requirements
    engram_topic: sdd/app-shell-login/spec
  design:
    status: completed
    artifact: design.md
    engram_topic: sdd/app-shell-login/design
  tasks:
    status: completed
    artifact: tasks.md
    engram_topic: sdd/app-shell-login/tasks
    notes: >-
      7 phases. S5 was SPLIT into 5A (router skeleton + session bootstrap +
      guards + logout + placeholder shell) and 5B (login screen), because the
      combined seam forecast ~770 authored lines — near the 810-line, 2x-budget
      overrun that auth-sesiones hit on its own P4 when strict-TDD coverage was
      underestimated. Review Workload Forecast flags two seams as
      "decision needed before apply": S4 at ~410 and S5a at ~430, both marginally
      over the 400-line budget, each with a proposed file-group split point if
      the measured diff confirms the overrun.
  apply:
    status: in_progress
    engram_topic: sdd/app-shell-login/apply-progress
    notes: >-
      Seam S1 (frontend error contract) shipped AHEAD of the tasks phase, at the
      user's request, because it was the one concrete defect already present in
      the codebase. Branch feat/shell-pr1-api-errors, off
      docs/sdd-planning-frontend. Commits: 33c7bb5 feat(web) typed ApiError,
      b91810f chore(biome) ignore design canvas assets. Created
      apps/web/src/api/errors.ts (ApiError with explicit fields — NOT TypeScript
      parameter properties, which interact badly with useDefineForClassFields
      under esbuild — plus isApiError); reworked client.ts with isErrorEnvelope()
      narrowing and toApiError(), falling back to code UNEXPECTED_RESPONSE for a
      non-envelope body (proxy HTML, truncated or invalid JSON) so the UI always
      receives a typed error; rewrote client.test.ts, intentionally superseding
      the old .rejects.toThrow('503') assertion exactly as spec and design
      recorded in advance. RED was verified for real (vitest could not resolve
      ./errors.js before it existed). Verified on branch: typecheck OK, lint OK,
      contract:check OK and byte-identical (S1 touches no API file, by design
      D14), api 63/63, web 9/9 (up from 2). 173 insertions / 11 deletions, well
      inside the 400 authored-line budget.
    s2_notes: >-
      Seam S2 (data + domain) landed on branch feat/shell-pr2-data-domain (off
      feat/shell-pr1-api-errors), commit 1eec0a0, ledger attempt settled
      "passed". All 9 Phase 2 tasks done under strict TDD with verified RED —
      including the integration RED, which failed against real Docker Postgres
      with `column "debe_cambiar_password" of relation "usuarios" does not
      exist` before the migration was applied. Added: schema column
      (NOT NULL DEFAULT false) + migration 0001_friendly_frightful_four.sql,
      UsuariosRepo.updatePassword (D6 — hash and flag in ONE UPDATE),
      SesionesRepo.deleteOthers (ne()), Usuario.debeCambiarPassword via the
      existing findValid JOIN (D1, zero extra queries), service.changePassword
      (D5-D7 + the D9 audit-hook-point comment), and the two error factories
      passwordChangeRequired() 403 / invalidCurrentPassword() 400.
      ORCHESTRATOR RE-VERIFIED INDEPENDENTLY: zero route source files touched;
      the only change under routes/ is a single `debeCambiarPassword: false`
      fixture line in routes/auth.test.ts, a mechanical consequence of widening
      the shared Usuario type, not a D14 violation. contract:check green and
      byte-identical. 245 authored lines (drizzle/** excluded) against a 400
      budget and a ~300 forecast. api 69/69 (was 63), web 9/9, integration
      12/12, typecheck + lint green.
    s3_notes: >-
      Seam S3 (API surface + server enforcement) landed on branch
      feat/shell-pr3-api-guard (off feat/shell-pr2-data-domain). Commits: 581b29e
      feat(api) enforcement + endpoint + regenerated contract, 3473007 test(api)
      integration round trip, dcf356d docs(sdd) checkboxes. All 6 Phase 3 tasks
      done under strict TDD with verified RED (3.1: "expected 200 to be 403" and
      "expected 'FORBIDDEN' to be 'PASSWORD_CHANGE_REQUIRED'"; 3.3: seven route
      failures, 403/404 before the route and the DTO field existed).
      Added: allowPasswordChangePending?: true config key (D3), request.sessionId
      decorated in onRequest from unsigned.value (D8 — verified that the unsigned
      cookie value IS sesiones.id, since findValid matches eq(sesiones.id, id) and
      deleteOthers matches ne(sesiones.id, exceptId)), the forced-change branch in
      preHandler at line 89 (D2 — after the auth === false return at 77, before
      the roles read at 92), POST /auth/password, and debeCambiarPassword in
      usuarioDto/toDto.
      ORCHESTRATOR RE-VERIFIED INDEPENDENTLY: D14 honored — git show --name-only
      on 581b29e lists apps/api/openapi.json and apps/web/src/api/schema.d.ts in
      the SAME commit as routes/auth.ts. Exactly two routes opt in (GET /auth/me
      line 106, POST /auth/password line 130); POST /auth/logout correctly has no
      entry. contract:check exit 0 and byte-identical, typecheck + lint green,
      api 82/82 (was 69), web 9/9, integration 13/13.
      BUDGET: 421 authored lines against 400 — 5% over, 345 of it test code
      covering 5 plugin scenarios + 9 route scenarios + 1 integration round trip.
      Consistent with the standing no-further-splits decision; not re-litigated.
      PRE-EXISTING DEFECT FOUND, NOT FIXED (out of S3 scope): the integration
      suite is flaky at roughly 50% under vitest's default file parallelism.
      apps/api/src/auth/repository.integration.test.ts:34 and
      apps/api/src/routes/auth.integration.test.ts:49 both run
      `truncate table sesiones, usuarios cascade` in beforeEach against the same
      database, so one file wipes the other's freshly inserted rows mid-test.
      VERIFIED PRE-EXISTING: both truncates are present at 840c15c, before S3.
      VERIFIED REPRODUCIBLE: two parallel runs gave 1 failed / 12 passed then
      13/13; three serial runs (--no-file-parallelism) gave 13/13 every time.
      Fix belongs in apps/api/vitest.integration.config.ts, which currently sets
      no pool or isolation options.
    s4_notes: >-
      Seam S4 (visual foundation) landed on branch feat/shell-pr4-visual-foundation
      (off fix/integration-test-isolation, which is off feat/shell-pr3-api-guard).
      Commits: 35bd1e1 deps, 6bc77fa tokens + global stylesheet, 1bfbbfb RTL
      cleanup, 6abc445 the four UI primitives, b16ff67 checkboxes, 27dbb5e
      orchestrator brand-mark correction.
      Q3 RESOLVED (was open_at_install). Orchestrator read both installed
      peerDependencies blocks directly rather than trusting the agent:
      @hookform/resolvers 5.9.1 declares zod "^3.25.0 || ^4.0.0", and
      @tanstack/react-router 1.170.32 declares react ">=18.0.0 || >=19.0.0".
      Both are PINNED EXACT (no caret) in apps/web/package.json so this cannot
      drift. zod ^4.4.3 added to apps/web to match apps/api.
      DEFECT FOUND AND FIXED BY THE ORCHESTRATOR: AuthCard's brand mark was built
      at 30x30 radius-8. That is docs/design.md line 44's SIDEBAR logo spec, and
      task 4.7's text had copied it by mistake. The approved login mockup
      docs/design/Main.dc.html line 32 renders the login mark at 44x44 radius-12
      on the same gradient. Mockups win — they are the Q1 resolution and the
      approved visual source of truth. Component and task text both corrected in
      27dbb5e, with a CSS comment warning against reverting it.
      UNPLANNED FIX, KEPT: apps/web/src/test/setup.ts had no RTL cleanup. RTL only
      self-registers cleanup when it detects a global afterEach, and this repo
      never sets vitest test.globals: true, so components leaked between tests and
      any file with 2+ render() calls hit false "multiple elements found"
      failures. afterEach(cleanup) added; this unblocks all future web component
      tests, not just S4's.
      VERIFIED: typecheck + lint green, contract:check exit 0 and byte-identical
      (S4 touches no API file), api 82/82, web 18/18 (was 9).
      BUDGET: 563 authored lines against 400 — 41% over, and materially larger
      than the 410 forecast the no-further-splits decision was reasoned on.
      Composition matters here: ~290 lines are CSS (169 module.css + 59 tokens.css
      + 62 global.css), 117 are tests, leaving ~156 lines of TypeScript. Reported
      to the user rather than split unilaterally, since no-further-splits stands.
    s5a_notes: >-
      Seam S5a (router skeleton + session bootstrap + guards) landed on branch
      feat/shell-pr5a-router-session, single commit ca849b3, all 16 tasks.
      ORCHESTRATOR RE-VERIFIED INDEPENDENTLY, focusing on the three decisions
      that could not be caught later by a test:
      (1) ROUTE NESTING CORRECT — routeTree.ts nests cambiarPasswordRoute as a
      DIRECT child of authLayout, sibling to shellLayout, so the forced-change
      guard cannot expel the user from the one screen that clears the flag.
      This was the loop hazard; it is avoided.
      (2) SESSION SEMANTICS CORRECT — session.ts returns null only on
      error.status === 401 and rethrows everything else, so a 500 is not read as
      "logged out". No createContext exists anywhere under apps/web/src, so D12
      holds: the session lives only in the react-query cache under ['session'].
      (3) D10 HELD — no routeTree.gen anywhere in the index, no
      @tanstack/router-plugin in package.json or the vite config. The tree is
      hand-written in routes/routeTree.ts.
      useLogout uses onSettled, so the redirect to /ingresar fires on failure as
      well as success (5A.13).
      VERIFIED: typecheck + lint green, contract:check exit 0 and byte-identical,
      api 82/82, web 27/27 (was 18; -2 from deleting the App.tsx scaffolding,
      +11 new).
      TWO REVIEW CONCERNS RAISED WITH THE USER, NOT SILENTLY ACCEPTED:
      (a) BUDGET — 736 changed lines (675 insertions + 61 deletions) against a
      400 budget and a ~430 forecast. Unlike S4, the bulk here is logic: 258 test
      lines and 80 CSS lines leave roughly 400 lines of logic-bearing TypeScript
      across router construction, two route guards, a query client that
      intercepts errors, and logout. The tasks phase had already split S5 (~770
      forecast) into S5a + S5b specifically to avoid this, and S5a alone came in
      at nearly the whole unsplit estimate. This is the "jump in scale" the
      no-further-splits rationale named as the thing that actually breaks review.
      (b) COMMIT GRANULARITY — all 16 tasks arrived as ONE commit, so the diff
      could not be reviewed or bisected incrementally.
      RESOLVED at the user's instruction: ca849b3 was unwound with a mixed reset
      and re-committed as 8 work units in dependency order — session query,
      query client, guards, route tree assembly, shell + logout, test helper +
      router integration tests, entry-point rewire, docs. Content is byte-for-byte
      identical to ca849b3 except the useLogout fix below; re-verified after the
      split (typecheck, lint, contract:check, api 82/82, web 27/27).
      IMPORT CYCLE FOUND AND FIXED BY THE ORCHESTRATOR: useLogout imported the
      router and queryClient module singletons, closing the cycle
      router -> routeTree -> index -> useLogout -> router. It worked only because
      `router` was read inside the onSettled callback rather than at module scope
      — too subtle to depend on, and it would break the moment anyone hoisted
      that read. The hook now takes both from context via useRouter() and
      useQueryClient(), which removes the cycle and makes it testable in
      isolation. Note the design had already anticipated the sibling cycle
      (queryClient <-> router, solved with the late-bound setRouter) but this
      third edge was not foreseen.
    s5b_notes: >-
      Seam S5b (login screen) landed on branch feat/shell-pr5b-login-screen in
      six work-unit commits, 8b8dc35 through 735094d. The commit-granularity
      instruction from the S5a postmortem was followed this time.
      ORCHESTRATOR RE-VERIFIED INDEPENDENTLY:
      (1) FEATURES BOUNDARY HELD — LoginForm.tsx imports only hookform, the four
      S4 primitives, its CSS module and the schema. No router, no react-query, no
      apiFetch. Its test renders with no router at all, which is the entire point:
      the form stays testable in isolation and useLogin owns the mutation and
      navigation.
      (2) retryAfter READ CORRECTLY — the mapper treats details.retryAfter as
      SECONDS and converts to minutes, matching accountLocked() at
      apps/api/src/lib/errors.ts:80-83. The agent read the real factory instead of
      assuming a unit, and narrows `details: unknown` defensively. A minutes/
      seconds mix-up here would have shown users a lockout 60x too short.
      (3) UI COPY MATCHES THE MOCKUP verbatim — "Correo electrónico",
      "Contraseña", "Ingresar" all appear in docs/design/Main.dc.html. Spanish
      here is correct: it is transcribed user-facing copy from the approved
      design, not persona drift. Identifiers and comments stay English.
      (4) ingresar.tsx beforeLoad throws redirect({ to: '/' }) when a session
      already resolves, so an authenticated user never sits on the login screen.
      VERIFIED: typecheck + lint green, contract:check exit 0 and byte-identical,
      api 82/82, web 40/40 (was 27).
      TEST-HELPER GAP FOUND AND FIXED BY THE AGENT: buildTestRouter wrapped only
      RouterProvider, never QueryClientProvider. It went unnoticed in S5a because
      no route component reached in those guard-redirect tests actually called a
      react-query hook. The first login test surfaced it as "No QueryClient set".
      Now buildTestRouter returns { router, queryClient } and renderRouter wraps
      both, matching how AppProviders composes the real app. This is the correct
      fix rather than a workaround.
      BUDGET: 504 changed lines against 400 — over, but materially healthier than
      S5a: 295 of those lines are tests, leaving roughly 209 lines of source
      across four small units. The agent reported the overrun itself and stated
      plainly that it had failed to measure mid-flight as instructed.
    s6_notes: >-
      Seam S6 (change-password screen) landed on branch
      feat/shell-pr6-change-password in four agent commits (2f48950..d17daa2)
      plus two orchestrator commits.
      ORCHESTRATOR RE-VERIFIED INDEPENDENTLY:
      (1) D5 BINDING CORRECT — cambiarPassword.tsx passes currentPasswordError
      only when isApiError(error) && error.code === 'INVALID_CURRENT_PASSWORD',
      and ChangePasswordForm renders it on the currentPassword TextField, not as
      a banner. The distinct 400 finally pays off here.
      (2) CACHE MERGED, NOT REPLACED — setQueryData spreads the existing usuario
      and flips only debeCambiarPassword, so id/nombre/email/rol survive.
      (3) ROUTE PARENTAGE HELD — still a child of authLayout with no beforeLoad
      of its own.
      DEFECT FOUND AND FIXED BY THE ORCHESTRATOR (96b6e7e): after a successful
      change the user was left sitting on the form. Clearing the flag only stops
      shellLayout from redirecting INTO /cambiar-password; that route hangs off
      authLayout, so nothing moved the user anywhere. The spec never specified
      post-success navigation, so the agent was right not to invent it, but the
      resulting flow was broken. onSuccess now navigates to '/', the way useLogin
      already did. The integration assertion was strengthened and PROVEN
      load-bearing: removing the navigate fails it with
      "expected '/cambiar-password' to be '/'".
      CONFIRM-PASSWORD FIELD ADDED AT THE USER'S DECISION (08acbe3): the approved
      mockup docs/design/CambiarPassword.dc.html shows a third field, "Repita la
      contraseña nueva", which task 6.1 omitted because it enumerated the API
      body. The agent flagged the divergence instead of silently choosing —
      correct behaviour. Raised with the user, who chose to add it. Rationale:
      this is the only screen where a typo is irreversible, since a mistyped new
      password locks the user out until an admin resets them.
      Implemented as changePasswordFormSchema, deliberately WIDER than
      changePasswordSchema. The request-body mirror stays exactly two fields, and
      ChangePasswordForm strips confirmPassword before calling onSubmit rather
      than relying on the server to discard it. Two tests pin this: one for the
      mismatch error, one asserting onSubmit receives exactly two fields.
      RED verified before GREEN: "Unable to find a label with the text of:
      Repita la contraseña nueva".
      A typecheck-only failure surfaced during this work and was fixed: `path:
      ['newPassword'] as const` produced a readonly tuple where zod wants a
      mutable PropertyKey[]. Vitest never sees it — only `pnpm typecheck` caught
      it, which is why the full gate runs and not just the suite.
      VERIFIED: typecheck + lint green, contract:check exit 0 and byte-identical,
      api 82/82, web 50/50 (was 48).
    seams:
      S1: done
      S2: done
      S3: done
      S4: done
      S5a: done
      S5b: done
      S6: done
  verify:
    status: completed
    verdict: PASS WITH FINDINGS
    notes: >-
      sdd-verify ran adversarially against the tip of the chain
      (feat/shell-pr6-change-password) on 2026-08-26. Verdict: PASS WITH
      FINDINGS — 1 SUGGESTION, 0 WARNING, 0 CRITICAL. It ran all five gates
      itself: typecheck and lint clean, contract:check byte-identical, api
      82/82, web 50/50, integration 13/13 deterministic. It confirmed D1-D16
      against the code rather than against these notes, including D14 via
      git show --name-only on 581b29e, the exactly-two allowPasswordChangePending
      routes, the guard's position between the auth-false return and the roles
      check, the one-UPDATE password write ordering, the absence of
      routeTree.gen and createContext, and the cambiarPassword-under-authLayout
      nesting. All five recorded deliberate deviations were confirmed real.
      ORCHESTRATOR CORRECTED THE VERIFIER: it reported tasks 7.1 and 7.2 as
      "correctly left unchecked, MANUAL, not attempted". That is wrong. 7.1 (the
      Neon migration) was applied before S3 shipped and verified in production;
      7.2 (the local integration run) was executed at 13/13 and is what surfaced
      the pre-existing parallelism flake. 7.4 was likewise already satisfied.
      All three are now checked in tasks.md with their evidence.
  diagnosed_flake:
    id: F2
    severity: flaky-test
    status: diagnosed
    observed: 2026-08-26
    diagnosed: 2026-08-26
    verdict: >-
      TIMEOUT, not an assertion failure. NO production bug. The cookie code is
      correct; the test's 5s budget was too small for a contended machine.
    what: >-
      `apps/api/src/routes/auth.test.ts` — "returns 200 with the usuario and a
      Set-Cookie with the expected attributes on success" failed ONCE, giving
      api 81/82. It had passed minutes earlier and no file under apps/api had
      been touched between the two runs.
    reproduced: >-
      REPRODUCED deliberately on 2026-08-26 at the user's request, after 12
      earlier clean runs had failed to surface it. Method: run the api suite
      against 16 busy `while` loops on an 8-core machine, keeping the FULL
      output of every run. Result: 1 failure in 12 contended runs. Baselines in
      the same script stayed green — 30 runs of the login file alone and 10
      uncontended full-suite runs, 0 failures.
    captured_evidence: >-
      `Error: Test timed out in 5000ms.` at src/routes/auth.test.ts:61, with the
      test itself measured at 5628ms. In the SAME run
      src/plugins/openapi.test.ts also timed out at 6306ms.
    decisive_observation: >-
      The openapi test does NO argon2 work at all, and it timed out too. So the
      deliberately-slow hash is NOT the special cause — the run's cost is
      dominated by cold-start module import (76s of import time in that run),
      and under CPU starvation ANY test in this suite can exceed 5s. This also
      explains why the failing test is the FIRST in its file: it absorbs module
      import, the argon2 native binding load, and the first Fastify app build.
      The original hypothesis (argon2 being slow) was too narrow.
    evidence_loss_cause: >-
      CORRECTED 2026-08-26. The original note blamed "rerun before capture".
      That is wrong. The failing command was recovered verbatim from the session
      transcript:
      `pnpm typecheck 2>&1 | tail -2 && pnpm lint 2>&1 | tail -1 &&
      pnpm contract:check 2>&1 | tail -1 && pnpm -r test 2>&1 | rg "Tests  |FAIL"
      | head -3`.
      vitest DID print the full diagnostic; the `rg "Tests  |FAIL" | head -3`
      filter discarded it at the moment of capture. The rerun destroyed nothing.
    lesson: >-
      Never filter test output down to counts when a failure is possible. Write
      the full output to a file and search the FILE. A filter that keeps only
      "FAIL" and the totals throws away the one thing that distinguishes a
      timeout from an assertion failure — which need opposite fixes.
    contention_source: >-
      CORRECTED 2026-08-26. The preceding typecheck/lint/contract:check were
      `&&`-sequential and had already finished, so they were NOT the concurrent
      load. The real overlap is inside `pnpm -r test` itself: workspace-concurrency
      is unset, so pnpm defaults to running the api and web suites CONCURRENTLY.
      web's jsdom environment is heavy (~20s, ~70s environment time) and the api
      login test does TWO real argon2 operations (setup hash + route verify) at
      memoryCost 19456 KiB. That overlap is the plausible path to vitest's 5s
      default timeout.
    ruled_out: >-
      Every assertion in the test is on a STATIC value (sid=, HttpOnly,
      SameSite=Lax, absence of Domain=), so none can flake on its own. The token
      is randomBytes(32).toString('base64url'), which has no '=' padding, so the
      `not.toMatch(/Domain=/i)` guard cannot false-positive on a random value.
      session.test.ts mutates process.env.NODE_ENV but restores it in both
      beforeEach and afterEach, and this test asserts nothing about Secure. That
      leaves exactly two candidates: a timeout, or login not returning 200
      (argon2 erroring under memory pressure). Both are environmental, not a
      logic bug in the cookie code.
    fix: >-
      apps/api/vitest.config.ts now sets testTimeout: 15_000 (~2.4x the worst
      observed 6306ms). This is the evidence-backed fix, not the blind one the
      earlier note warned against: raising a timeout is only safe BECAUSE the
      captured output proved a timeout. Had it been an assertion failure, this
      same change would have hidden a real bug — which is exactly why the fix
      waited for evidence.
    why_not_other_fixes: >-
      Precomputing the argon2 setup hash was considered and rejected: the
      openapi test proves argon2 is not the driver, so that would treat a
      symptom that is not the cause. Raising the timeout cannot mask a logic
      bug — every assertion still runs unchanged and a genuine hang still fails.
    scope_note: >-
      Only apps/api/vitest.config.ts was changed. apps/web and the integration
      config were left alone: there is no evidence of a timeout in either, and
      the whole point of this finding is not to change things without evidence.
      If web ever flakes the same way under CI contention, apply the same fix —
      but capture the output first.
  resolved_finding:
    id: F1
    severity: suggestion
    status: closed
    closed: 2026-08-26
    closed_by: 3743bbb
    finding: >-
      The app-shell spec scenario "Login background matches documented token"
      (#16233c) has no test. The value is implemented correctly — AuthCard's
      .screen paints var(--color-sidebar) and tokens.css defines it — but no
      test asserts it, so a token drifting away from docs/design.md would go
      unnoticed by the suite.
    attempted: >-
      A src/styles/tokens.test.ts was written and works: 53/53 green, and proven
      load-bearing by changing --color-sidebar and observing the failure. It
      reads the stylesheets from disk, because jsdom never applies a CSS Module's
      stylesheet (getComputedStyle reports nothing) and vitest resolves CSS
      `?raw` imports to an empty string.
    blocked_on: >-
      It fails pnpm typecheck: apps/web has no @types/node, and the test imports
      node:fs. Closing this needs @types/node as a web devDependency, imported as
      node:process rather than the global so tsconfig's types: ["vite/client"]
      stays narrow and app code still cannot reach Node APIs by accident.
    resolution: >-
      CLOSED on 2026-08-26 by commit 3743bbb, at the user's request after an
      initial deferral. @types/node was added as an apps/web devDependency and
      the test now lives at src/styles/tokens.test.ts. Web suite went 50 -> 53.
    resolution_approach: >-
      The narrow types: ["vite/client"] was NOT widened. Tests compile under a
      new apps/web/tsconfig.test.json (types: ["vite/client", "node"]) while
      tsconfig.json excludes test files, so all app source is still compiled
      without Node globals. `pnpm typecheck` in apps/web now runs both configs.
    resolution_pitfall: >-
      tsconfig.test.json must inherit include: ["src"]. Narrowing it to test
      files alone drops src/app/router.tsx, whose
      `declare module '@tanstack/react-router'` augmentation types the router
      context — losing it degrades useRouteContext() to `any` and makes
      unrelated files fail with implicit-any errors. This was hit and fixed
      during the work; do not "tidy" that include back to a narrow list.
    resolution_evidence: >-
      Both guards proven load-bearing, not assumed: changing --color-sidebar to
      #16233d failed "defines the documented login background", and a
      process.env reference added to src/routes/index.tsx failed the app config
      with TS2591. Full gate green afterwards — lint, contract:check, api and
      web typecheck, api 82/82, web 53/53.
    honest_caveat: >-
      This test's real value is modest. It guards against token drift; it does
      NOT prove the screen looks right. A layout could break completely with the
      token untouched and it would still pass.
    engram_topic: sdd/app-shell-login/verify-report
  archive:
    status: pending
    engram_topic: sdd/app-shell-login/archive-report

# Orchestrator gatekeeper ran after spec and design. Five drifts were found and
# reconciled in the spec's favour only where the spec was right; where the design
# had read the actual code, the spec was corrected to match it.
reconciliation:
  - id: R1
    was: "spec named the forced-change error code MUST_CHANGE_PASSWORD"
    now: "PASSWORD_CHANGE_REQUIRED (design D4)"
    why: >-
      Existing codes are state descriptions (ACCOUNT_LOCKED, ACCOUNT_INACTIVE,
      RATE_LIMITED), not imperatives. The design's name matches that convention.
  - id: R2
    was: "spec returned 401 INVALID_CREDENTIALS for a wrong current password"
    now: "400 INVALID_CURRENT_PASSWORD (design D5)"
    why: >-
      The session is valid; a 401 would trip the SPA's global session-expiry
      recovery and discard the user's typed input. 400 maps to a field error.
  - id: R3
    was: "spec allowlisted three routes (password, logout, me) by hardcoded URL"
    now: >-
      Opt-in route config key allowPasswordChangePending on exactly two routes
      (GET /api/auth/me, POST /api/auth/password) — design D3.
    why: >-
      VERIFIED against apps/api/src/routes/auth.ts:66 — POST /api/auth/logout
      already declares config: { auth: false } and unsigns the cookie itself, so
      it skips both hooks and never reaches the forced-change check. A config
      opt-in also mirrors the existing auth:false / roles pattern and is
      default-deny: a route added later is blocked until it opts in.
  - id: R4
    was: "spec used UNKNOWN_ERROR as the unparseable-body fallback code"
    now: "UNEXPECTED_RESPONSE (design, apps/web/src/api/errors.ts)"
    why: "More precise: the body was unparseable, not the error unknown."
  - id: R5
    was: >-
      spec prose carried hash_contraseña forward from the archived auth-sesiones
      spec; design.md titled the change backlog #4
    now: "hash_contrasena; backlog #2.1"
    why: >-
      VERIFIED against apps/api/src/db/schema.ts:22 and
      apps/api/drizzle/0000_old_omega_flight.sql:13 — the shipped column is
      ASCII. This delta promotes into the live spec at archive time, so the
      error had to be corrected here or it would become the contract.

open_questions:
  - id: Q1
    status: resolved
    resolved: 2026-08-25
    question: >-
      Visual direction for the login and change-password screens was unapproved.
      All three .dc.html wireframes referenced by docs/design.md are absent from
      the repository, so the screens had to be derived from design tokens alone.
    resolution: >-
      Mockups built from docs/design.md tokens, committed to docs/design/ and
      published at
      https://claude.ai/code/artifact/adbd0a58-ce25-4fdb-b2ab-4b560ee8f85d —
      Main.dc.html (login), LoginError.dc.html (invalid credentials),
      CambiarPassword.dc.html (forced change), canvas.json. Every hex in the
      mockups was checked against docs/design.md: 16 of 17 appear verbatim, the
      17th being #ffffff, which the document names as "blanco". One deliberate
      deviation, recorded in docs/design.md: the login card uses the MODAL
      shadow (0 18px 50px rgba(22,35,60,.4)) rather than the card shadow, because
      it floats on the dark #16233c field where a 7%-opacity shadow is invisible.
      Card radius stays 14px. The published Design System artifact also yielded
      the sidebar navigation labels and user-card format, now recorded in
      docs/design.md, which seam S5 needs.
    owner: user
  - id: Q2
    status: accepted
    question: >-
      Rate limiting on POST /api/auth/password is deferred; the endpoint already
      requires a valid session. Revisit if gestion-usuarios adds an
      unauthenticated reset path.
  - id: Q3
    status: open_at_install
    question: >-
      Pin versions at install: @hookform/resolvers must support zod v4 (the repo
      is on ^4.4.3); @tanstack/react-router must support React 19. Neither was
      installed or verified during design.
